//Reto encontrar numeros par
var numeros = prompt('introduce un número entero');
long numero = convert.toInt64(console.readline ());
long sobrante = numero % 2;

if(sobrante == 0)
{
    console.writeline("el numero es par");
}